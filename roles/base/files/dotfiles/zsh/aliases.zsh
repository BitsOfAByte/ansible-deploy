# Lazy aliases for common commands
alias cls="clear"

# Make neofetch use the custom logo
alias neofetch="neofetch --source .config/neofetch/logo.txt --ascii_colors 213"

# Add sync command
alias sync="ansible-pull -U https://github.com/BitsOfAByte/ansible-deploy.git --ask-become-pass"